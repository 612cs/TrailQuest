# 文档与仓库差异跟踪

本文档用于记录“既有论文、任务书、开题报告”与“当前仓库实现”之间的重要差异。

按照当前写作原则，论文正文继续严格保留既有材料中的主线内容。

本文件不用于删改论文口径，而是用于后续实现补齐与证据补齐。

## 一、技术路线差异清单

| 项目 | 既有材料中的要求 | 当前仓库可见事实 | 处理口径 | 后续动作 |
| --- | --- | --- | --- | --- |
| 大模型服务 | 明确写为 `DeepSeek` | 后端当前可见配置为 `ai.dashscope`，默认模型为 `qwen3.5-plus` 与 `qwen-plus` | 论文继续按 DeepSeek 写 | 后续代码实现需向 DeepSeek 路线补齐或提供正式替换说明 |
| Function Calling | 明确写为核心实现机制 | 当前仓库可见 AI 检索与结构化解析链路，但未看到严格意义上的正式 Function Calling 对接实现 | 论文继续按 Function Calling 写 | 后续实现需补齐显式 Function Calling 能力 |
| 预测服务部署方式 | 明确写为 `Python Flask` 封装随机森林微服务 | 当前仓库可见景观预测主链路位于 Java 后端中，随机森林分类器也在 Java 代码中实现 | 论文继续按 Python Flask 写 | 后续实现需补齐独立 Python Flask 服务或形成正式对齐方案 |
| 前端样式体系 | 明确写为 `Tailwind CSS + DaisyUI` | 仓库可见 Tailwind CSS，未在前端依赖中直接看到 DaisyUI | 论文继续保留 DaisyUI 口径 | 后续确认是否补齐依赖与实际使用证据 |

## 二、功能实现差异清单

| 项目 | 既有材料中的要求 | 当前仓库可见事实 | 处理口径 | 后续动作 |
| --- | --- | --- | --- | --- |
| 图片识别定位与 POI 推荐 | 开题报告明确写入目标效果 | 当前仓库中尚未发现足够直接的完整实现证据 | 论文主线保留 | 后续补齐实现或补齐明确的实现依据 |
| 天气与景观综合预测 | 任务书、开题和现有论文均有明确叙述 | 当前仓库已存在天气与景观相关接口和数据对象，但与既有材料中的部署方式不完全一致 | 论文保留原口径 | 后续统一技术实现与文档口径 |
| Redis 高频缓存策略 | 既有材料写入较强的缓存优化主线 | 当前仓库中 Redis 的可见使用范围较为有限，强证据主要集中在上传链路 | 论文保留原主线 | 后续补齐路线、热点数据和高频访问缓存实现 |
| 管理端治理能力 | 现有论文中保留管理员模块 | 当前仓库后端已存在管理员相关控制器与数据对象，但前端管理端能力仍需进一步核实 | 论文保留管理员模块 | 后续继续补齐可视化管理端与完整治理流程 |

## 三、测试与证据差异清单

| 项目 | 既有材料中的表述 | 当前仓库可见事实 | 风险说明 | 后续动作 |
| --- | --- | --- | --- | --- |
| 性能提升约 30% | 现有论文已明确写入 | 当前仓库可见测试脚本与测试结构，但暂未看到与该数字一一对应的完整测试报告留档 | 论文继续保留 | 后续补齐可复核的性能测试报告 |
| AI 成本降低约 65% | 现有论文已明确写入 | 当前仓库可见 AI 会话与推荐链路，但暂未看到可直接复核的成本台账材料 | 论文继续保留 | 后续补齐调用统计与成本测算依据 |
| 预测准确性结论 | 现有论文写入较积极的测试结论 | 当前仓库有预测器测试类，但与论文中的量化口径尚未完全打通 | 论文继续保留 | 后续补齐预测指标、样本来源与评估方法说明 |

## 四、仓库可直接补强的正向事实

以下内容不是差异点，而是可以安全补入论文的工程事实。

其一，项目已经采用 monorepo 方式组织前端与后端代码。

其二，前端已经形成首页、搜索、社区、个人中心、发布、聊天、路线详情和图片漫游等页面结构。

其三，后端已经形成用户、路线、评论、上传、配置、AI 会话、天气和景观相关接口与数据对象。

其四，项目已经接入单元测试、端到端测试和后端测试脚本，为测试章节补充工程细节提供了依据。

其五，项目已经具备 OSS 上传链路、聊天会话管理和景观预测结果展示等工程化补充内容。

## 五、后续补齐优先级

第一优先级为 DeepSeek、Function Calling 和 Python Flask 随机森林服务三项技术路线对齐。

第二优先级为图片识别定位、POI 推荐、Redis 高频缓存和管理员完整治理链路补齐。

第三优先级为性能数据、成本数据和预测准确性结论的证据补齐。

本文件后续将作为“论文写作不改口径、系统实现持续补齐”的总跟踪表使用。
